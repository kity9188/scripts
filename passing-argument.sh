#! /usr/bin/bash
echo $0 $1 $2 $3 '> echo $1 $2 $3'

#To see the difference, directly run ./<filename.sh>
# or pass the arguments like: ./<filename.sh> Hello Kaise Ho
# By adding $0 and running the script again with same arguments,
# will get the script name printed on screen as well.
